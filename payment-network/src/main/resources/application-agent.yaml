server:
  port: ${AGENT_PORT:8081}

spring:
  datasource:
    url: jdbc:h2:file:./data/agent-${AGENT_ID:default}
    driver-class-name: org.h2.Driver
    username: sa
    password:

app:
  role: agent
  agent:
    id: ${AGENT_ID:AGENT1}
    name: ${AGENT_NAME:Default Agent}
  master:
    url: ${MASTER_URL:http://localhost:8080}
  sync:
    enabled: true
    interval: 30000  # 30 seconds